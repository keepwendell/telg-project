# 260916 · 播放器时间戳与循环问题

## 对话要点

- 用户实测：句级时间戳偏移（从第三句开始不准）、Listen Mode 删除 Repeat 后布局残留、单句循环按键位置/语义、选中句高亮范围。
- 根因：`-c copy` 拼接保留 mp3 编码 padding 累积偏移 + gap 帧对齐误差 + `format duration` 估算误差。
- 修复：逐句归一化 WAV → 解码实测 → filter concat 统一重编码；gap 改 WAV；单句循环开启即跳当前句首并回卷；合成后 `<audio>` 强制重载消除高亮错位；句间静音归属上一句高亮不闪断。
- 提交：`30f5a63`（时间戳链路 + 循环入口 + 高亮）、`d44359f`（循环生效 + 音频重载）。
- 用户再次实测：问题"依然存在"（2026-09-16），已立档 `reports/260916-播放器跳转与循环问题/`，待用户环境核对（素材是否重合成 / 前后端是否最新 / 浏览器缓存）。
